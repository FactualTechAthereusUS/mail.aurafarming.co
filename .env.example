# AuraMail Configuration

# SendGrid Email Service (Optional - falls back to SMTP if not configured)
SENDGRID_API_KEY=your_sendgrid_api_key_here
USE_SENDGRID=false

# API URL for frontend
NEXT_PUBLIC_API_URL=https://api.aurafarming.co

# JWT Secret (change in production)
JWT_SECRET=AuraMail2024_SuperSecret_JWT_Key_For_Auth

# Database Configuration (Already configured in DigitalOcean)
DB_HOST=localhost
DB_USER=auramail
DB_PASSWORD=AuraMail2024!
DB_NAME=mailserver

# Server Configuration
PORT=4000
NODE_ENV=development
